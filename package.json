{
  "name": "the-hexican-archive",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ The-Hexican-Archive",
    "create-local": "graph create --node http://localhost:8020/ The-Hexican-Archive",
    "remove-local": "graph remove --node http://localhost:8020/ The-Hexican-Archiven",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 The-Hexican-Archive"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.37.6",
    "@graphprotocol/graph-ts": "^0.29.1"
  }
}
